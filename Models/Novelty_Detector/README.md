# Novelty Detector

Runtime-only known/new detection migrated from `novelty_lab`.

It takes a DINO embedding and returns either:

- `known`: keep the gallery label
- `new`: assign or preview a temporary group like `new_1`

This folder intentionally does not include dataset preparation, plots, or training experiments.
